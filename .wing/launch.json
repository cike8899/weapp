{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceRoot}/app.js",
            "cwd": "${workspaceRoot}"
        },
        {
            "name": "调试微信小程序",
            "type": "weapp",
            "request": "launch",
            "project": "${workspaceRoot}"
        }
    ]
}